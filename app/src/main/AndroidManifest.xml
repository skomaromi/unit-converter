<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.github.skomaromi.unitconverter">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".GreeterActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".ResultActivity"
            android:label="@string/result_activitytitle" />
        <activity
            android:name=".LengthConvActivity"
            android:label="@string/global_length" />
        <activity
            android:name=".SpeedConvActivity"
            android:label="@string/global_speed" />
        <activity
            android:name=".MassConvActivity"
            android:label="@string/global_mass" />
        <activity
            android:name=".TemperatureConvActivity"
            android:label="@string/global_temperature" />
    </application>

</manifest>